{
  "name": "@vitrine/common",
  "private": true,
  "version": "0.0.1",
  "description": "vitrine: common",
  "scripts": {
    "cleaning": "rm -rf node_modules",
    "compile": "tsc --noEmit",
    "build": "yarn compile",
    "deploy": "echo 'NO DEPLOY SCRIPT AVAILABLE ON COMMON'",
    "format": "prettier --write '{.,src/**}/*.{js,jsx,ts,tsx}'",
    "lint": "tslint -p .",
    "start": "echo 'NO START SCRIPT AVAILABLE ON COMMON'",
    "test": "echo 'TODO: test'"
  },
  "dependencies": {
    "uuid": "3.3.2"
  },
  "devDependencies": {
    "@types/uuid": "3.4.4"
  }
}
